객체지향 설계의 핵심은 클래스가 아니라 객체 간의 협력이다.

# 역할 (Role)
객체가 협력 안에서 맡는 추상적인 역할로 구현은 중요하지 않다 

# 책임(Responsibility)
객체가 그 역할로서 반드시 보장해야 하는 행동이다

# 협력(Collaboration)
객체가 책임을 수행하기 위해 다른 객체와 메시지를 주고 받는 관계이다 
협력은 명시적이어야한다